<%= render "shared/header" %>
<h1><%= @user.name %>さんのマイページ</h1>

<%# ユーザーの情報 %>
<div class="user-show">
  <div class="user-box">
    <h2 class="name">
      <%= "#{@user.name}" %>
    </h2>
    <div class="user-img-content">
      <% if @user. %>
      <%= image_tag @user.image ,class:"user-box-img" %>
    </div>
    <div class="user-price-box">
      <span class="user-price">
        <%= "¥ #{@user.price}" %>
      </span>
      <span class="user-postage">
        <%= "#{ShippingFee.find(@user.shipping_fee_id).name}" %>
      </span>
    </div>

    <% if user_signed_in? && @user.order.blank? %>
      <% if current_user.id == @user.user_id %>
        <%= link_to "商品の編集", edit_user_path(@user), method: :get, class: "user-red-btn" %>
        <p class="or-text">or</p>
        <%= link_to "削除", user_path(@user), method: :delete, class:"user-destroy" %>
      <% else %>
        <%= link_to "購入画面に進む", user_orders_path(@user.id) ,class:"user-red-btn"%>
      <% end %>
    <% end %>

    <div class="user-explain-box">
      <span><%= "#{@user.user_detail}" %></span>
    </div>
    <table class="detail-table">
      <tbody>
        <tr>
          <th class="detail-user">出品者</th>
          <td class="detail-value"><%= "#{@user.user.nickname}" %></td>
        </tr>
        <tr>
          <th class="detail-user">カテゴリー</th>
          <td class="detail-value"><%= "#{@user.category.name}" %></td>
        </tr>
        <tr>
          <th class="detail-user">商品の状態</th>
          <td class="detail-value"><%= "#{Condition.find(@user.condition_id).name}" %></td>
        </tr>
        <tr>
          <th class="detail-user">配送料の負担</th>
          <td class="detail-value"><%= "#{ShippingFee.find(@user.shipping_fee_id).name}" %></td>
        </tr>
        <tr>
          <th class="detail-user">発送元の地域</th>
          <td class="detail-value"><%= "#{Prefecture.find(@user.prefecture_id).name}" %></td>
        </tr>
        <tr>
          <th class="detail-user">発送日の目安</th>
          <td class="detail-value"><%= "#{ShippingDay.find(@user.shipping_day_id).name}" %></td>
        </tr>
      </tbody>
    </table>
    <div class="option">
      <div class="favorite-btn">
        <%= image_tag "star.png" ,class:"favorite-star-icon" ,width:"20",height:"20"%>
        <span>お気に入り 0</span>
      </div>
      <div class="report-btn">
        <%= image_tag "flag.png" ,class:"report-flag-icon" ,width:"20",height:"20"%>
        <span>不適切な商品の通報</span>
      </div>
    </div>
  </div>
  <%# /商品の概要 %>

  <div class="comment-box">
    <form>
      <textarea class="comment-text"></textarea>
      <p class="comment-warn">
        相手のことを考え丁寧なコメントを心がけましょう。
        <br>
        不快な言葉遣いなどは利用制限や退会処分となることがあります。
      </p>
      <button type="submit" class="comment-btn">
        <%= image_tag "comment.png" ,class:"comment-flag-icon" ,width:"20",height:"25"%>
        <span>コメントする<span>
      </button>
    </form>
  </div>
  <div class="links">
    <a href="#" class="change-user-btn">
      ＜ 前の商品
    </a>
    <a href="#" class="change-user-btn">
      後ろの商品 ＞
    </a>
  </div>
  <a href="#" class="another-user"><%= "#{Condition.find(@user.condition_id).name}" %>をもっと見る</a>
</div>

<%= render "shared/footer" %>
